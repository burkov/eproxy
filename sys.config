[
  {eproxy, [
    {server_port, 1080}
  ]},

  {lager, [
    {colored, true},
    {handlers, [
      {lager_console_backend, [debug, {lager_pretty_formatter, [severity, " ", time, " ", module, line, message, "\n"]}]}
    ]}
  ]}
].
